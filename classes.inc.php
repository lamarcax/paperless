<?
  /*
    The classes used by this website
	user
        log
        folder
        document
        keywords
        profile
        revisions
        revision
        download_tuples
  */
  require_once('db.inc.php');
  require_once('functions.inc.php');

/*
   The class user is imported into the class list
*/

require_once('classes/user.inc.php');

/*
   The class log is imported into the class list
*/

require_once('classes/log.inc.php');

/*
   The class folder is imported into the class list
*/

require_once('classes/folder.inc.php');

/*
   The class document is imported into the class list
*/

require_once('classes/document.inc.php');

/*
   The class keywords is imported into the class list
*/

require_once('classes/keyword.inc.php');

/*
   The class profile is imported into the class list
*/

require_once('classes/profile.inc.php');

/*
   The class revision is imported into the class list
*/

require_once('classes/revision.inc.php');

/*
   The class revisions is imported into the class list
*/

require_once('classes/revisions.inc.php');

/*
   The class download_tuples is imported into the class list
*/

require_once('classes/download_tuples.inc.php');
?>
